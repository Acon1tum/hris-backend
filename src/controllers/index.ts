export default class IndexController {
    getUsers(req: import('express').Request, res: import('express').Response) {
        // Logic for getting users
    }

    createUser(req: import('express').Request, res: import('express').Response) {
        // Logic for creating a user
    }
}